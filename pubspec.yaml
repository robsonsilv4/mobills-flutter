name: mobills_flutter
description: Clone do Mobills com Flutter.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # cupertino_icons: ^0.1.2
  flutter_launcher_icons: ^0.7.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - images/coins.png

flutter_icons:
  android: "ic_launcher"
  ios: true
  image_path: "assets/icon/icon.png"